import styled from 'styled-components';

const Container = styled.div`
	height: 100vh; // container spans entire pagfe height
	width: 100vw; // container spans entire page width
	display: flex; // flexible container
	flex-direction: column; // organized top-bottom
`;

export default Container;

// <Container></Container>
